import { Signal } from "./modules/signal.js";

/**
 * Assign the namespace Object if it already exists or instantiate it as an object if not.
 */
export const cub = game.cub;

/**
 * Start the module
 */
Signal.lightUp();
